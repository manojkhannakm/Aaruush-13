package com.webarch.aaruush13.components;

import android.content.Context;
import android.util.AttributeSet;
import android.view.View;

public class LoadingView extends View {

//    private Movie loading;
//    private long startTime = System.currentTimeMillis();
//    private int xOffset;

    public LoadingView(Context context, AttributeSet attrs) {
        super(context, attrs);

//        loading = Movie.decodeStream(getResources().openRawResource(R.raw.loading));
//        post(new Runnable() {
//
//            @Override
//            public void run() {
//                xOffset = (getWidth() - loading.width()) / 2;
//            }
//
//        });
    }

//    @Override
//    protected void onDraw(Canvas canvas) {
//        super.onDraw(canvas);
//
//        loading.setTime((int) ((System.currentTimeMillis() - startTime) % loading.duration()));
//        loading.draw(canvas, xOffset, 0);
//        invalidate();
//    }

}